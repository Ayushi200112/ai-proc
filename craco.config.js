// craco.config.js
/*module.exports = {
    webpack: {
      configure: (webpackConfig) => {
        // Add a rule to ignore source map warnings for face-api.js
        webpackConfig.module.rules.push({
          test: /\.ts$/,
          use: ['source-map-loader'],
          enforce: 'pre',
          exclude: /node_modules\/face-api.js/,  // Ignore source map for face-api.js
        });
        return webpackConfig;
      },
    },
  };*/
  